extends layout
 
block content
 div#content
  h1 #{title}
  if error
   h2 #{error}
  if info
   h2 #{info}
  form(method="post")
   label 用户名：  
   input(type="text" name="name")
   br
   label 密码：  
   input(type="password" name="password")
   br
   input(type="submit" value="登录")